package model;
public class Question extends Issue {
    public Question(String title, String description, String urlImage, String status) {
        super(title, description, urlImage, status);
    }
}